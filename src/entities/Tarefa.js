export default class Tarefa {
    constructor (id, titulo, concluida, usuarioId) {
        this.id = id
        this.titulo = titulo
        this.concluida = concluida
        this.usuarioId = usuarioId
    }
}